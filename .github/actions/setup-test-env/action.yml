name: "Setup Test Environment (Enterprise)"
description: "Start PostgreSQL, Temporal and the AutoKitteh EE container"

inputs:
  descope-project-id:
    description: "Descope project ID"
    required: true
  browser:
    description: "Browser under test"
    required: true
  autokitteh-image:
    description: "AutoKitteh EE Docker image tag"
    required: true
  rsa-private-key:
    description: "RSA private key for JWT signing"
    required: true
  rsa-public-key:
    description: "RSA public key for JWT verification"
    required: true

runs:
  using: "composite"
  steps:
    # ───────────────────────────────────────────────────────────────
    - name: Clean up old containers / volumes
      shell: bash
      run: |
        echo "~~~~~~Clean up old containers / volumes~~~~~~"
        docker rm -f postgres-db temporal-dev 2>/dev/null || true
        docker volume rm $(docker volume ls -qf dangling=true) 2>/dev/null || true

    - name: Create Docker network
      shell: bash
      run: |
        echo "~~~~~~Create Docker network~~~~~~"
        docker network create temporal-net || true

    # ───────────────────────────────────────────────────────────────
    # 1. PostgreSQL
    - name: Start PostgreSQL
      shell: bash
      run: |
        echo "~~~~~~Start PostgreSQL~~~~~~"
        POSTGRES_CONTAINER_ID=$(docker run -d \
          --network temporal-net \
          --name postgres-db \
          -p 5432:5432 \
          -e POSTGRES_USER=postgres \
          -e POSTGRES_PASSWORD=postgres \
          postgres:15-alpine)
        echo "POSTGRES_CONTAINER_ID=$POSTGRES_CONTAINER_ID" >> "$GITHUB_ENV"

        echo "Waiting for PostgreSQL…"
        until docker exec "$POSTGRES_CONTAINER_ID" pg_isready -U postgres >/dev/null 2>&1; do
          printf '.'
          sleep 2
        done

        docker exec -u postgres "$POSTGRES_CONTAINER_ID" psql -U postgres -c "CREATE DATABASE temporal;"
        docker exec -u postgres "$POSTGRES_CONTAINER_ID" psql -U postgres -c "CREATE DATABASE temporal_visibility;"
        docker exec -u postgres "$POSTGRES_CONTAINER_ID" psql -U postgres -c "CREATE DATABASE autokitteh;"

    # ───────────────────────────────────────────────────────────────
    # 2. Temporal
    - name: Start Temporal dev server
      shell: bash
      run: |
        echo "~~~~~~Start Temporal dev server~~~~~~"
        TEMPORAL_CONTAINER_ID=$(docker run -d \
          --network temporal-net \
          --name temporal-dev \
          -p 7233:7233 \
          -e DB=postgres12 \
          -e DB_PORT=5432 \            # <<< added back
          -e POSTGRES_USER=postgres \
          -e POSTGRES_PWD=postgres \
          -e POSTGRES_SEEDS=postgres-db \
          temporalio/auto-setup:1.24.2)
        echo "TEMPORAL_CONTAINER_ID=$TEMPORAL_CONTAINER_ID" >> "$GITHUB_ENV"

        echo "Waiting for Temporal…"
        for i in {1..60}; do
          docker ps --filter id=$TEMPORAL_CONTAINER_ID --format '{{.ID}}' | grep -q "$TEMPORAL_CONTAINER_ID" || {
            echo "Temporal exited!"; docker logs $TEMPORAL_CONTAINER_ID; exit 1; }
          curl -fs http://localhost:7233/api/v1/namespaces >/dev/null 2>&1 && { echo "Temporal ready!"; break; }
          [ $i -eq 60 ] && { echo "Temporal failed to start"; docker logs $TEMPORAL_CONTAINER_ID; exit 1; }
          printf '.'; sleep 2
        done

    # ───────────────────────────────────────────────────────────────
    # 3. AutoKitteh EE
    - name: Start AutoKitteh Enterprise
      shell: bash
      env:
        DESCOPE_PROJECT_ID: ${{ inputs.descope-project-id }}
        RSA_PRIVATE_KEY:   ${{ inputs.rsa-private-key }}
        RSA_PUBLIC_KEY:    ${{ inputs.rsa-public-key }}
      run: |
        echo "~~~~~~Start AutoKitteh Enterprise~~~~~~"
        CONTAINER_ID=$(docker run -d \
          --network temporal-net \
          -p 9980:9980 \
          --add-host=host.docker.internal:host-gateway \
          -e AK_DB__TYPE=postgres \
          -e AK_DB__DSN=postgres://postgres:postgres@postgres-db:5432/autokitteh?sslmode=disable \
          -e AK_TEMPORALCLIENT__HOSTPORT=temporal-dev:7233 \
          -e AK_TEMPORALCLIENT__NAMESPACE=default \
          -e AK_AUTHHTTPMIDDLEWARE__USE_DEFAULT_USER=false \
          -e AK_AUTHLOGINHTTPSVC__DESCOPE__ENABLED=true \
          -e AK_AUTHLOGINHTTPSVC__DESCOPE__PROJECT_ID="$DESCOPE_PROJECT_ID" \
          -e AK_HTTP__CORS__ALLOWED_ORIGINS=http://localhost:8000 \
          -e AK_AUTHSESSIONS__ALLOWED_CORS_COOKIE=true \
          -e AK_AUTHJWTTOKENS__ALGORITHM=rsa \
          -e AK_AUTHJWTTOKENS__RSA__PRIVATE_KEY="$RSA_PRIVATE_KEY" \
          -e AK_AUTHJWTTOKENS__RSA__PUBLIC_KEY="$RSA_PUBLIC_KEY" \
          -e AK_SECRETS__PROVIDER=db \
          ${{ inputs.autokitteh-image }} up)
        echo "CONTAINER_ID=$CONTAINER_ID" >> "$GITHUB_ENV"

        echo "Waiting for AutoKitteh…"
        for i in {1..60}; do
          curl -fs http://localhost:9980/healthz >/dev/null 2>&1 && { echo "AutoKitteh ready!"; break; }
          [ $i -eq 60 ] && { echo "AutoKitteh failed to start"; docker logs $CONTAINER_ID; exit 1; }
          printf '.'; sleep 2
        done

    # ───────────────────────────────────────────────────────────────
    - name: Tail AutoKitteh logs (background)
      shell: bash
      run: |
        docker logs -f $CONTAINER_ID > docker-logs.txt &
        echo "LOGS_PID=$!" >> "$GITHUB_ENV"

    - name: Install Safari deps
      if: inputs.browser == 'Safari'
      shell: bash
      run: npx playwright install-deps webkit

    - name: Show container summary
      shell: bash
      run: docker ps --filter id=$CONTAINER_ID --filter id=$TEMPORAL_CONTAINER_ID --filter id=$POSTGRES_CONTAINER_ID
