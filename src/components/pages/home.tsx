import React from "react";
import { AppWrapper, MapMenuFrameLayout } from "@components/templates";

export const Home = () => {
	return (
		<AppWrapper>
			<MapMenuFrameLayout />
		</AppWrapper>
	);
};
