export enum UserStatus {
	active = 1,
	disabled = 3,
	invited = 2,
	unspecified = 0,
}
export enum UserStatusType {
	active = "active",
	disabled = "disabled",
	invited = "invited",
	unspecified = "unspecified",
}
