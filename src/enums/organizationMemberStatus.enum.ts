export enum MemberStatus {
	active = 1,
	declined = 3,
	invited = 2,
	unspecified = 0,
}
export enum MemberStatusType {
	active = "active",
	declined = "declined",
	invited = "invited",
	unspecified = "unspecified",
}
