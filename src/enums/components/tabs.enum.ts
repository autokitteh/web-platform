export enum EOutputTabs {
	output = "output",
	tab = "tab",
}

export enum EProjectTabs {
	codeAndAssets = "code-and-assets",
	connections = "connections",
	triggers = "triggers",
	variables = "variables",
}
