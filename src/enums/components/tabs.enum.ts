export enum EOutputTabs {
	output = "output",
	tab = "tab",
}

export enum EProjectTabs {
	codeAndAssets = "Code & Assets",
	connections = "Connections",
	triggers = "Triggers",
	variables = "Variables",
}
