export enum ESidebarMenu {
	newProject,
	myProjects,
	dashboard,
	connections,
	stats,
	settings,
}

export enum ESidebarHrefMenu {
	myProjects = "my-projects",
	dashboard = "dashboard",
	connections = "connections",
	stats = "stats",
	settings = "settings",
}
