export enum ESidebarMenu {
	newProject,
	myProjects,
	dashboard,
	connections,
	stats,
	settings,
}

export enum ESidebarHrefMenu {
	myProjects = "projects",
	dashboard = "dashboard",
	connections = "connections",
	stats = "stats",
	settings = "settings",
}
