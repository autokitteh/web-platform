export enum SidebarMenu {
	newProject,
	myProjects,
	dashboard,
	connections,
	stats,
	settings,
}

export enum SidebarHrefMenu {
	projects = "projects",
	dashboard = "dashboard",
	connections = "connections",
	stats = "stats",
	settings = "settings",
}
