export enum ButtonVariant {
	filled = "filled",
	filledGray = "filledGray",
	light = "light",
	outline = "outline",
	flatText = "flatText",
}

export enum TopbarButton {
	build = "Build",
	deploy = "Deploy",
	deployments = "Deployments",
}
