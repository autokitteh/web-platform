export enum ButtonVariant {
	default = "default",
	filled = "filled",
	light = "light",
	outline = "outline",
}

export enum TopbarButton {
	build = "Build",
	deploy = "Deploy",
	viewStats = "View Stats",
}
