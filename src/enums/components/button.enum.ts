export enum ButtonVariant {
	default = "default",
	filled = "filled",
	outline = "outline",
	light = "light",
}

export enum TopbarButton {
	build = "Build",
	deploy = "Deploy",
	viewStats = "View Stats",
}
