[[headers]]
  for = "/*.map"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Content-Type = "application/json"
    X-Robots-Tag = "noindex"

# Prevent access to source maps
[[redirects]]
  from = "/*.map"
  to = "/404"
  status = 404
  force = true